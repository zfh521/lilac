#(optional)override jdbc url as in-memory db for unit test
jdbc.driver=org.h2.Driver
jdbc.url=jdbc:h2:mem:lilac;DB_CLOSE_DELAY=-1
jdbc.username=sa
jdbc.password=
#druid settings
druid.initialSize=1
druid.minIdle=1
druid.maxActive=20
druid.oracle.validationQuery = SELECT 1 FROM DUAL
